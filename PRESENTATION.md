# InsurGen™ AI 2.0 — מצגת אינטראקטיבית

מסמך זה מגדיר את המבנה, העיצוב, ההתנהגות והתחזוקה של המצגת האינטראקטיבית
`index.html`.

---

## 1. סקירה כללית

| | |
|---|---|
| **שם** | InsurGen™ AI 2.0 |
| **סוג** | מצגת מכירה / הצגת מוצר, אינטראקטיבית, בפורמט HTML |
| **קהל יעד** | מקבלי החלטות בחברות ביטוח בישראל |
| **שפה** | עברית (RTL); מונחים טכניים ושמות מוצר באנגלית |
| **מקור התוכן** | `InsurGenֳ AI- Product Short Spec (1).pdf` (13 עמודים) |
| **מספר שקפים** | 13 |
| **פורמט** | קובץ HTML עצמאי אחד + תיקיית `assets/` |
| **תלויות** | אין. Vanilla JS, ללא build, ללא framework. פונטים מ‑Google Fonts (עם fallback ל‑offline) |
| **הרצה** | פתיחה ישירה בדפדפן (`file://`) — עובד מקומית ללא שרת. גם כאתר סטטי (ראו §10) |
| **Repo** | `github.com/adiro-collab/insurgen_presentation` (private) |
| **פריסה** | Render — Static Site, ללא build. ראו §10 |

---

## 2. מבנה הקבצים

```
InsurGen_Presenation/
├── index.html      ← המצגת (מסמך HTML מלא ועצמאי)
├── PRESENTATION.md           ← המסמך הזה
└── assets/
    ├── solugen-logo.png      לוגו SoluGen AI (לבן + טורקיז, שקוף)   ~1100×170
    ├── architecture.png      שקף הארכיטקטורה — צילום מעמוד 9 ב‑PDF    2880×1620
    ├── partners.png          לוגואי ענן AWS · Azure · GCP (שקוף)     ~1334×186
    ├── logos/                10 לוגואי לקוחות, לבן על שקוף (שקף 4)
    │   ├── harel.png  phoenix.png  migdal.png  aig.png  clal.png
    │   └── menora.png tmura.png  madanes.png  shomera.png  hapool.png
    ├── adir.jpg              תמונת פרופיל — Adir Oren                480×480
    ├── nechemya.jpg          תמונת פרופיל — Nechemya Kanelsky        480×480
    ├── niv.jpg               תמונת פרופיל — Niv Ben Shimon           480×480
    └── yana.jpg              תמונת פרופיל — Yana Rubanovich          480×480
```

> **חשוב:** יש להשאיר את `index.html` ואת `assets/` באותה תיקייה.
> אם מעבירים את ה‑HTML לבד, התמונות יישברו.

---

## 3. מבנה המצגת

| # | id | כותרת | פרק | תוכן עיקרי |
|---|----|-------|-----|-----------|
| 1 | `s1` | InsurGen™ AI 2.0 | פתיחה | לוגו SoluGen AI, טאגליין, שם המוצר, כותרת משנה בעברית |
| 2 | `s2` | על מה נדבר היום | אג׳נדה | 6 סעיפים ממוספרים (לחיצים) |
| 3 | `s3` | עלינו | 01 · עלינו | פסקת הגדרה + לוגואי ספקי ענן (AWS / Azure / GCP) |
| 4 | `s4` | לקוחות מהתעשייה | 01 · עלינו | קיר לוגואים של 10 לקוחות (תמונה) |
| 5 | `s5` | הכאב | 02 · הכאב | 2 מוקדי עלות: הפסדי פיצויים · עומס תפעולי ידני |
| 6 | `s6` | הפתרון | 03 · הפתרון | תומך החלטה / מקבל החלטה + הבהרה |
| 7 | `s7` | רכיבי ליבה | 03 · הפתרון | תהליך בן 4 שלבים: UNDERSTAND · DECIDE · ACT · GOVERN |
| 8 | `s8` | מה מייחד אותנו | 04 · ייחודיות | 3 כרטיסים: ניסיון · טכנולוגיה · AI אחראי + הבהרה |
| 9 | `s9` | ארכיטקטורת המערכת | 04 · ייחודיות | תמונת ארכיטקטורה במסך מלא (מהמצגת המקורית) |
| 10 | `s10` | סיפורי הצלחה · 01–03 | 05 · סיפורי הצלחה | רכב חובה · חבויות גוף · אמבולטורי |
| 11 | `s11` | סיפורי הצלחה · 04–06 | 05 · סיפורי הצלחה | צד ג׳ + רכוש · חיות מחמד · חיתום פוליסות |
| 12 | `s12` | הצגת מערכת | 06 · הדגמה | DEMO + 2 קישורים חיים למערכת |
| 13 | `s13` | תודה רבה | סיום | 3 אנשי צוות עם תמונות ופרטי קשר |

### פירוט השקפים

#### שקף 1 — פתיחה
- לוגו **SoluGen AI** (תמונה, ממורכז)
- טאגליין: `Reinventing Insurance Operations with Gen AI`
- כותרת ראשית: **InsurGen™ AI 2.0** (בצבע הטורקיז `#16D9A8` — כמו "Gen" בלוגו)
- כותרת משנה: `פתרון בינה מלאכותית עבור מגזר הביטוח`
- כותרת תחתונה: `PRODUCT MANAGEMENT · V2.0`
- אנימציה: קו טורקיז שנמתח עם הכניסה לשקף
- הלוגו הקבוע בפינה **מוסתר** בשקף זה

#### שקף 2 — אג׳נדה
6 פריטים ממוספרים בעיגולי טורקיז. **כל פריט לחיץ** וקופץ לשקף המתאים:

| פריט | קופץ לשקף |
|------|-----------|
| עלינו | 3 |
| הכאב | 5 |
| הפתרון | 6 |
| ייחודיות הפתרון | 8 |
| סיפורי הצלחה | 10 |
| הדגמת מערכת | 12 |

#### שקף 3 — עלינו
- פסקה: *SoluGen AI הינה חברת מוצר אשר משנה את הדרך שבה חברות ביטוח מיישבות
  תהליכי תביעות, חיתום ותפעול — באמצעות פתרון בינה מלאכותית מותאם אישית למגזר הביטוח.*
- למטה: לוגואי **AWS · Microsoft Azure · Google Cloud** (תמונה שקופה) + תווית `שותפים` (לבן)

#### שקף 4 — לקוחות מהתעשייה
קיר לוגואים אינטראקטיבי — 10 לוגואים נפרדים בתוך כרטיס אחד
(הראל והפניקס מוגדלים בשורה עליונה):
הראל · הפניקס · מגדל · AIG · כלל ביטוח ופיננסים · מנורה מבטחים · תמורה ·
מדנס (SM) · שומרה · הפול.

- **מצב רגיל:** לבן, ~68% שקיפות.
- **ב‑hover:** מואר ל‑100%, הגדלה קלה (×1.08) וזוהר טורקיז.
- הלוגואים הם גרסת **מונוכרום לבנה** (הצבע היחיד שקיים במצגת המקור).
  להצגת **צבע מותג מקורי ב‑hover** יש להוסיף ל‑`assets/logos/` קובץ צבעוני
  בשם `<name>-color.png` (לדוגמה `harel-color.png`) ולעדכן את חוקי ה‑CSS של `.lw`.

#### שקף 5 — הכאב
משפט פתיחה: *חברות הביטוח מתמודדות עם שני מוקדי עלות משמעותיים:*

| # | כותרת | תוכן |
|---|-------|------|
| 01 | הפסדי פיצויים | תשלומי יתר של פיצויי תביעות וחוסר דיוק בתהליכי קבלת ההחלטות |
| 02 | עומס תפעולי ידני | מערכי תפעול גדולים המבוססים על מספר רב של מיישבי תביעות המטפלים ידנית בתהליכים מורכבים ורפטטיביים |

#### שקף 6 — הפתרון
פסקה: *מוצר AI פרואקטיבי לניהול חיי התביעה מקצה לקצה, המאפשר לחברת הביטוח לעבוד
במצב של תומך החלטה או מקבל החלטה, בהתאם לסוג התביעה ולמדיניות הארגון.*

| מצב | תיאור |
|-----|-------|
| **תומך החלטה** | עוזר AI אישי פרואקטיבי שמכיר את התיק לעומק, מנגיש מידע ויזואלי ומסייע לאנשי המקצוע לקבל החלטות |
| **מקבל החלטה** | סוכן AI מבוסס מנוע יישוב תביעה בעולמות תביעות / חיתום — מנתח את התיק ומקבל החלטה עפ״י חוקה / מחשבון |

הבהרה תחתונה: *הפתרון אינו מחליף את המערכת התפעולית של הלקוח, אלא יושב מעליה כמיישב תביעות AI.*

#### שקף 7 — רכיבי ליבה
תהליך בן 4 שלבים (השלב הרביעי, GOVERN, מסומן בכחול כשכבה רוחבית):

| # | שלב | תיאור |
|---|-----|-------|
| 1 | **UNDERSTAND** | להבין את כל המידע בתביעה — מסמכים, נתונים, היסטוריה ומערכות הליבה |
| 2 | **DECIDE** | לנתח את התיק, להפעיל חוקים וולידציות, ולהמליץ או לקבל החלטה |
| 3 | **ACT** | לבצע פעולות פרואקטיביות ולהניע את התביעה קדימה מול המערכות והגורמים |
| 4 | **GOVERN** | שימוש אחראי, שקוף ומבוקר ב‑AI — ראיות, בקרת ודאות, תיעוד ובקרה אנושית |

#### שקף 8 — מה מייחד אותנו

**ניסיון**
- ניסיון מוכח בפרודקשן: הפתרונות רצים בחברות ביטוח מובילות, ומטפלים במעל 500,000 תביעות בשנה
- מוכנות מראש: תשתית מוצרית מובנית ל‑85% ממוצרי ותהליכי הביטוח בישראל
- הבנה עסקית: צוות של מומחי תוכן בעולמות הביטוח

**AI תהליכי**
- מבין את התיק באופן יזום – קורא את המסמכים, הנתונים וההיסטוריה ומייצר תמונת 360° בלי שהמשתמש צריך לדעת מה לשאול
- מפעיל לוגיקה עסקית ולא רק LLM – שילוב של GenAI, מנוע חוקים, ולידציות, הצלבות ו־Decision Logic
- מוביל להחלטה ולא רק למידע – המלצת יישוב, מסלול ירוק / STP, הפניה לבדיקה עמוקה או דחייה בהתאם לחוקים

**טכנולוגיה**
- מערך אג׳נטי אשר מזהה, מנתח, ומציג את המידע בדחיפה למיישב
- ניהול תהליך ומנועים: אוטומציה מקצה לקצה, מנוע חוקים מתקדם, ניהול חריגים
- פריסה: ניתן לפרוס את הפתרון בכל אחד מספקי הענן
- AI אחראי: בקרה, ניטור, אימות מידע, מנגנוני עצירה

הבהרה תחתונה: *היתרון אינו איפה ה־AI יושב במסך, אלא כמה מהתהליך העסקי הוא באמת יודע להבין — ומה הפעולה הדרושה הבאה.*

#### שקף 9 — ארכיטקטורת המערכת
תמונה במסך מלא (`assets/architecture.png`) — צילום מדויק של שקף הארכיטקטורה
מהמצגת המקורית (עמוד 9). כולל: תחומי ביטוח, מקורות מידע, טכנולוגיית הליבה
(Rules / Business / Settlement / Responsible AI engines + Data AI Preprocessing)
ואינטגרציות ארגוניות. השקף מוצג ללא כותרת/לוגו/מסגרת (bleed).

#### שקפים 10–11 — סיפורי הצלחה
6 כרטיסים, כל אחד עם תיאור + תיבת **ערך עסקי**:

| # | תחום | תג |
|---|------|-----|
| 01 | רכב חובה | תביעות |
| 02 | חבויות גוף | תביעות |
| 03 | אמבולטורי | תביעות |
| 04 | צד ג׳ + רכוש | תביעות |
| 05 | חיות מחמד | תביעות |
| 06 | חיתום פוליסות | חיתום |

#### שקף 12 — הצגת מערכת
- כותרת `הצגת מערכת` · `DEMO` · `InsurGen™ AI` + איור רובוט
- **2 קישורים חיים** (נפתחים בלשונית חדשה), כיתוב `פתיחת הפתרון ↗`:

| סדר | כיתוב | קישור |
|-----|-------|-------|
| 1 | ביטוח חובה | `https://demo.solugen.ai/insurance/case-selection` |
| 2 | חבויות גוף | `https://ayalon-demo.solugen.ai/login` |

#### שקף 13 — תודה רבה
4 כרטיסי צוות (תמונה עגולה בטבעת טורקיז, לפי סדר התצוגה):

| סדר | שם | תפקיד | טלפון | מייל |
|-----|-----|-------|-------|------|
| 1 | Adir Oren | CEO | 054-5615656 | adiro@solugen.ai |
| 2 | Nechemya Kanelsky | Innovation Lead | 050-6730770 | nk@solugen.ai |
| 3 | Niv Ben Shimon | Sales Director | 050-8949450 | nivb@solugen.ai |
| 4 | Yana Rubanovich | Head of Product | 050-7502511 | yanar@solugen.ai |

כותרת תחתונה: קישור ל‑`www.solugen.ai`

---

## 4. מערכת העיצוב

מצגת **חד‑ערכתית כהה** (single-theme dark), במכוון — כך היא נראית נכון על מקרן/מסך
ותואמת למצגת המקור.

### צבעים (CSS custom properties)

| Token | ערך | שימוש |
|-------|-----|-------|
| `--bg` | `#080d18` | רקע בסיס |
| `--bg-2` | `#0d1324` | רקע משני / תגי מספר |
| `--panel` | `#0f1c25` | רקע כרטיסים |
| `--panel-2` | `#14262e` | כרטיס ב‑hover |
| `--line` | `rgba(22,217,168,.16)` | גבול עדין |
| `--line-2` | `rgba(22,217,168,.36)` | גבול מודגש |
| `--teal` | `#16d9a8` | **צבע המותג הראשי** — כותרות, מספרים, הדגשות |
| `--teal-2` | `#54f2ca` | הדגשה בהירה (מילים מודגשות) |
| `--blue` | `#22a5dc` | accent משני — שלב GOVERN |
| `--ink` | `#e9f3f1` | טקסט ראשי |
| `--ink-2` | `#a4b8b5` | טקסט משני |
| `--ink-3` | `#6b8380` | טקסט עמום / eyebrows |

> הטורקיז `#16D9A8` זהה ל‑"Gen" בלוגו SoluGen AI.

### טיפוגרפיה

| תפקיד | פונט | משקלים | שימוש |
|-------|------|--------|-------|
| כותרות | **Assistant** | 400 / 600 / 700 / 800 | כותרות שקף, שמות כרטיסים, שמות אנשים |
| גוף | **Rubik** | 300 / 400 / 500 / 600 | פסקאות, תיאורים, פריטי רשימה |
| נתונים / תוויות | **JetBrains Mono** | 400 / 500 | eyebrows, מספרים, תוויות אנגלית (UNDERSTAND וכו׳), טלפונים |

נטענים מ‑Google Fonts עם fallback stacks (`Segoe UI`, `Arial`, `system-ui`, `Menlo`).

### פריסה

- **במה (stage)** ביחס 16:9, עד `1360×765px`, ממורכזת, פינות מעוגלות, זוהר טורקיז ברקע + רשת נקודות עדינה.
- כל המידות פנימיות ביחידות **Container Query** (`cqw` / `cqh`) — התוכן מתאים את עצמו לגודל הבמה בכל מסך.
- `border-radius: 14px` לכרטיסים, `18px` לבמה.
- אנימציית כניסה לכל אלמנט עם `data-reveal` (fade + הזזה קלה), עם השהיה מדורגת לפי סדר ה‑DOM (`--i`). מכבד `prefers-reduced-motion`.

---

## 5. ניווט ואינטראקציה

הניווט בכיוון עברי (RTL): **קדימה = שמאלה**, **אחורה = ימינה**.

| פעולה | קדימה (הבא) | אחורה (הקודם) |
|-------|-------------|---------------|
| לחצן על המסך | צד **שמאל**, חץ פונה שמאלה ‹ | צד **ימין**, חץ פונה ימינה › |
| מקלדת | `←` · `↓` · `PageDown` · `Space` | `→` · `↑` · `PageUp` |
| לחיצה על שולי המסך | חצי **שמאלי** | חצי **ימני** |
| החלקה במגע | החלקה שמאלה | החלקה ימינה |

| מקש | פעולה |
|-----|-------|
| `G` | סקירת כל השקפים (רשת) — לחיצה על שקף קופצת אליו |
| `F` | מסך מלא |
| `?` | חלון עזרה עם קיצורי מקלדת |
| `Esc` | סגירת חלון (סקירה / עזרה / מסך מלא) |
| `Home` / `End` | שקף ראשון / אחרון |

רכיבים נוספים:
- **סרגל תחתון (HUD):** פס התקדמות, מונה שקפים (`03 / 13`, לחיץ → סקירה), לחצני סקירה/מסך‑מלא/ניווט.
- **לוגו קבוע** בפינה השמאלית‑עליונה של כל שקף (מוסתר בשקף 1 ובשקף 9).
- **Deep-linking:** ה‑URL מתעדכן ל‑`#N` — אפשר לשתף קישור לשקף מסוים או לרענן ולהישאר במקום.
- החצים הופכים לעמומים ולא פעילים בשקף הראשון/האחרון.

---

## 6. נכסים (assets)

| קובץ | מקור | ממדים | הערות |
|------|------|-------|-------|
| `solugen-logo.png` | `SoluGen AI Logo - Dark (2).svg` (חתוך לשורת ה‑wordmark בלבד, ללא הטאגליין) | ~1100×170 | שקוף. לבן + "Gen" טורקיז |
| `architecture.png` | PDF עמוד 9, רינדור ×4 | 2880×1620 | שקף מלא |
| `logos/*.png` (×10) | PDF עמוד 4 — כל לוגו חולץ, אוחד ל‑לבן על שקוף, נחתך | רוחב ≤900 | שקוף. לוגואי לקוחות לשקף 4 |
| `partners.png` | PDF עמוד 3 — 3 לוגואים חולצו ועובדו ל‑אפור בהיר על שקוף | ~1334×186 | שקוף |
| `adir.jpg` / `niv.jpg` / `yana.jpg` | PDF עמוד 13 — חילוץ ותמונות, חיתוך לריבוע | 480×480 | JPEG |
| `nechemya.jpg` | תמונה שסופקה, חיתוך לריבוע (הוסרה טבעת מקורית) | 480×480 | JPEG |

---

## 7. עריכה ותחזוקה

### עקרונות מבנה
- כל שקף = `<section class="slide" id="sN" data-title="…" data-section="…">`.
  - `data-title` — הכותרת שמופיעה בסקירת השקפים (`G`).
  - `data-section` — שם הפרק בסקירה.
- שקף במסך מלא (תמונה בלבד) = מוסיפים `slide--bleed` ל‑class; הלוגו הקבוע נעלם אוטומטית.
- `data-reveal` על אלמנט = אנימציית כניסה. הסדר נקבע לפי סדר ה‑DOM.

### פעולות נפוצות

| רוצים ל… | עושים |
|----------|-------|
| לשנות טקסט | לערוך ישירות ב‑HTML בתוך ה‑`<section>` המתאים |
| להחליף תמונה | להחליף את הקובץ ב‑`assets/` תוך שמירה על אותו שם, **או** לעדכן את `src` |
| לשנות צבע מותג | לעדכן `--teal` ב‑`:root` (בראש ה‑`<style>`) |
| לשנות סדר שקפים | להזיז את בלוק ה‑`<section>` כולו; לעדכן את מספרי `data-goto` באג׳נדה (שקף 2) |
| להוסיף שקף | להעתיק `<section class="slide" id="sN">…`, למקם, ולהגדיר `data-title` / `data-section` |
| לשנות קישורי דמו | שקף 12, `class="demo-link"` — לעדכן `href` ואת ה‑`dl-label` |
| לשנות פרטי צוות | שקף 13, בלוקי `class="person"` |
| לשנות כיוון/התנהגות ניווט | CSS: `.nav-arrow--prev` / `.nav-arrow--next` (מיקום) · JS: מקטע `keydown`, ה‑`deck.addEventListener('click'…)` וה‑`touchend` |

### הערות טכניות / החלטות
- **אין תו טקסט לחצים.** בהקשר RTL הדפדפן הופך אוטומטית תווי‑ציטוט כמו `❮ ❯`.
  לכן כל החצים (ניווט + מחברי שלבים בשקף 7) מצוירים כ‑**SVG** או ב‑CSS.
- הקובץ הוא **מסמך HTML מלא ותקין** (`<!doctype>`, `<head>` עם `charset=utf-8`,
  `<body>`) — כדי שיוצג נכון בפתיחה מקומית כ‑`file://`, כולל עברית.
- כל התמונות הן קבצים חיצוניים ב‑`assets/` (לא Base64) — קל להחליף ולתחזק.
- ללא build ו‑ללא תלויות. הפונטים נטענים מ‑CDN אך יש fallback מלא ל‑offline.

---

## 8. הרצה והצגה

1. לפתוח את `index.html` בדפדפן (Chrome / Safari / Edge / Firefox עדכני).
2. `F` למסך מלא.
3. לנווט עם `←` (קדימה) / `→` (אחורה), רווח, או הלחצנים על המסך.
4. בשקף 12 — הקישורים נפתחים בלשונית חדשה מול סביבות ה‑demo החיות.

**הצגה ממכשיר אחר / קליקר:** רוב הקליקרים שולחים `PageDown` / `PageUp`
(או `←` / `→`) — תואם.

---

## 9. מיפוי למצגת המקור

השקפים תואמים 1:1 לעמודי `InsurGenֳ AI- Product Short Spec (1).pdf` (13 עמודים).
הבדלים מכוונים מהמקור:

- שקף 1: הלוגו כתמונה + הכותרת בטורקיז מלא (במקום גרדיאנט).
- שקף 3: לוגואי הענן כתמונה שקופה; התווית "שותפים" בלבן ומוגדלת.
- שקף 4: קיר לוגואים אינטראקטיבי — 10 לוגואים נפרדים עם אפקט hover (במקום טקסט).
- שקף 7: 4 כרטיסים קומפקטיים וממורכזים; ללא משפט ה‑GOVERN התחתון.
- שקף 8 › טכנולוגיה › שורה ראשונה: *"מערך אג׳נטי אשר מזהה, מנתח, ומציג את המידע בדחיפה למיישב"*.
- שקף 9: תמונת הארכיטקטורה מה‑PDF במסך מלא (במקום שחזור ב‑HTML).
- שקף 12: נוספו 2 קישורים חיים למערכת (`פתיחת הפתרון`).
- שקף 13: תמונות הפנים האמיתיות מה‑PDF (במקום ראשי תיבות).

---

## 10. פריסה (Render — Static Site)

האתר הוא **HTML סטטי בלבד** — אין build, אין שרת, אין Node. יש לפרוס אותו כ‑
**Static Site** ולא כ‑Web Service.

### דרך א׳ — Blueprint (מומלץ, אוטומטי)
הריפו כולל `render.yaml`. ב‑Render:
1. **New → Blueprint**
2. לבחור את הריפו `insurgen_presentation`
3. **Apply** — Render קורא את `render.yaml` ומקים Static Site ללא build.

### דרך ב׳ — הגדרה ידנית (Static Site קיים)

| שדה | ערך |
|-----|-----|
| **Environment** | `Static Site` (לא "Web Service") |
| **Branch** | `main` |
| **Build Command** | `rm -rf build && mkdir build && cp index.html build/ && cp -R assets build/` |
| **Publish Directory** | `build` |

> אפשר גם בלי build כלל: **Build Command** ריק, **Publish Directory** = `.`

### נקודות שגורמות לשגיאה
- **`Publish directory build does not exist`** — Publish Directory הוגדר `build` אבל אין build שמייצר את התיקייה.
  פתרון: או להגדיר Build Command כמו בטבלה למעלה (מייצר `build/`), או לשנות Publish Directory ל‑`.`.
- **בחירת "Web Service" במקום "Static Site"** — Render יחפש `start command` / רנטיים ויכשל.
- **Build Command עם `npm install`** — אין `package.json`, ייכשל.
- הקובץ הראשי הוא **`index.html`** (שוּנה מ‑`insurgen-ai-2.0.html`) כדי ש‑Render יגיש אותו בשורש.
- הפונטים נטענים מ‑`fonts.googleapis.com` — עובד על דומיין HTTPS אמיתי (ויש fallback).
